name: Update README

on:
  push:
    branches:
      - main

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Install Tree
        run: sudo apt-get install -y tree

      - name: Generate Project Structure
        run: |
          echo "# Project Structure" > README.md
          echo "" >> README.md
          tree -L 2 >> README.md || echo "tree command not found; install it" >> README.md

      - name: Commit and Push Changes
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}  # Use GitHub Secret for security
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git diff --exit-code || (
            git add README.md
            git commit -m "Update README.md with project structure"
            git remote set-url origin https://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@github.com/AtakanG7/presentations.git
            git push origin main
          )
